#include<stdio.h>
int main()
{
      
	    int a,b,c ,avg;
        printf("enter first number :");
		scanf("%d",&a);
		printf("enter second number:");
		scanf("%d",&b);
		printf("enter third  number:");
		scanf("%d",&c);

		 avg = a+b+c;
		 avg = avg/3;
		 printf("avg = %d \n", avg);

     return 0; 
 }               
